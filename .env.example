VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_BACKEND_URL=

# Supabase Edge Functions / Backend AI Configuration
# The OpenAI key must NEVER be exposed to the browser (do not use VITE_ prefix).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
